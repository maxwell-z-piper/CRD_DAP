
# Archived Script-03 forensic diagnostics

The former 03a--03g scripts are development/forensic utilities, not production
science stages.

They were used to isolate the KcwiKit finite-negative corruption and develop
the independent atmospheric-mask machinery. The production atmospheric mask is
now created and validated by CRD_DRP before CRD_DAP begins.

Keep the historical scripts here (or rely on Git history) so the investigation
remains reproducible, but do not run them as part of the normal seven-stage
CRD_DAP sequence.
