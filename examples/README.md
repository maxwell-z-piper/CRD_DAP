# Examples

`target_config_example.py` is currently a copy of the full configuration template. Once real targets are running through the pipeline, this directory can contain sanitized example configurations and small demonstration notebooks/scripts that do not require proprietary or very large science data.
